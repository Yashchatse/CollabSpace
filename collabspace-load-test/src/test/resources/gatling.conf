gatling {
  core {
    encoding = "utf-8"
    elFileBodiesCache = true
  }
  http {
    pooledConnectionIdleTimeout = 60000
    requestTimeout = 10000
    enableGA = false
  }
  data {
    writers = [console, file]
    console {
      light = false
    }
  }
}