server:
  port: 8761

spring:
  application:
    name: collabspace-eureka

eureka:
  client:
    register-with-eureka: false   # eureka server doesn't register itself
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0


---

## Verify It's Working

Run `CollabspaceEurekaApplication.java` and open:

